
import React from "react";
import { HeroSectionBasic } from "@/components/ui/galaxy-hero-demo";

const HeroSection = () => {
  return (
    <section id="hero" className="relative w-full h-screen">
      <HeroSectionBasic />
    </section>
  );
};

export default HeroSection;
